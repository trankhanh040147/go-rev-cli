# v0.1
(TODO: Update what've done here)

---
# v0.2
#go-rev-cli 
**Features**:
- Diff base with custom branch/commit:
	 - compare against a branch
	 - compare against a specific commit
	 - compare MR-style: changes since branching from main (ex: main...HEAD)
- Show context used (like Cursor)
- Change default best free model (gemini-pro-2.5)
- Allow user to select available models based on their API KEY (free tier)
- Rename tool because it's purpose is not only Go (rename to rev-cli or something better)
(Recommend features here)
**Todo:**
- [ ] Create PROGRESS.MD
---
# v0.3
**Features:**
- After enter review command, user can:
	- Select branch
	- Enter prompt (optional)
	- Choose model
	- ...
- User can custom prompt (code style,...)
- Review every language
---
# Future features
(Recommend features, break phases if possible)